(load-file "~/Internet/Git/Emacs/lambda-line/lambda-line.el")

(setq lambda-line-position 'top)
(setq lambda-line-space-top +0.30)
(setq lambda-line-space-bottom -0.30)
(setq lambda-line-symbol-position 0.1)
(setq lambda-line-prefix t)

(lambda-line-mode 1)
